# 相邻 Skill 路由

先完成当前答案，再在结尾补相邻 skill 建议，不要一上来就分流。

## 路由表

- 用户要战略诊断、年度经营计划、战略汇报：
  先用 `zh-huawei-strategy-principles` 判断逻辑质量，再用 `zh-strategy-blm-dste-execution`
- 用户要市场细分、目标市场选择、机会排序：
  先用 `zh-huawei-market-principles` 判断逻辑质量，再用 `zh-market-management-execution`
- 用户要路标评审、版本规划、产品线取舍：
  先用 `zh-huawei-product-principles` 判断规划质量，再用 `zh-product-planning-execution`
- 用户要研发执行、阶段评审、里程碑、跨部门协同：
  用 `zh-product-development-ipd-execution`
- 用户要线索流转、商机治理、合同到回款：
  用 `zh-sales-ltc-execution`
- 用户要服务台、问题关闭、升级机制、VOC 回灌：
  先用 `zh-huawei-service-principles` 判断设计质量，再用 `zh-service-itr-execution`
- 用户要试点、推广、治理、PMO 节奏：
  用 `zh-transformation-pmo-execution`

## 常见组合

- `方法总入口 -> 战略原则 -> 战略执行 -> 产品规划 -> IPD 执行`
- `方法总入口 -> 市场原则 -> 市场执行 -> 产品规划`
- `方法总入口 -> 销售 LTC -> 服务 ITR`
- `方法总入口 -> 变革 PMO`
